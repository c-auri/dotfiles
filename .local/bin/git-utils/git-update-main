#!/bin/bash

# Updates the default branch from remote without switching to it

source "$HOME/.local/bin/git-utils/git-lib"
require_git_repo
require_remote

default_branch=$(get_default_branch)
if [[ "$(git branch --show-current)" == "$default_branch" ]]
then
    git pull
else
    git fetch "$(get_remote)" "$default_branch:$default_branch"
fi
