# Machine-specific env vars, aliases, and functions.
# See ~/docs/shell-startup.md for context.

export PROJECTS_DIR=
export LAPTOP_RESOLUTION=
export LAPTOP_BRIGHTNESS=

export PNPM_HOME=

# Idle dim and auto-lock. Empty or invalid falls back to the default.
# Only the exact value 1 disables anything.
# See ~/.local/share/lockscreen/README.md.
export IDLE_DIM_AFTER_SECONDS=
export IDLE_DIM_DISABLE=
export IDLE_LOCK_DISABLE=
export IDLE_LOCK_SECONDS_AFTER_DIM=

# Prompt name and its tput color, defaults to whoami in 14.
export USER_ALIAS=
export USER_COLOR=

# Add machine-specific aliases and functions below.

