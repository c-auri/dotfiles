# Applies to both dotfiles repos, since both use $HOME as their work tree.

# Profile drop-ins are machine-local by default, so that a stray `add -A`
# cannot commit secrets. Context drop-ins that belong in a repo opt in below.
.profiles/*.sh
!.profiles/work.sh

.config/rofi/emoji/recent.txt
